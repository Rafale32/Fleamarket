<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="com.fleamarket.mapper.MainDetailMapper">
	<cache />


	<!-- <insert id="insertBoard" parameterType="Board">
		insert into board(
		seq, title, writer, contents, regdate, hitcount, fname
		)values(
		board_seq.nextval, #{title},#{writer},#{contents},sysdate,0,#{fname}
		)
	</insert> -->

	<select id="recommendList" resultType="MainDetailDTO">
		select * from itemboard where recommend_ornot = 'TRUE'
	</select>

	<select id="recommendItem" resultType="TemDTO">
		select * from item where item_no in(select item_no from itemboard where RECOMMEND_ORNOT = 'true')
	</select>
	
	<select id="categoryList" resultType="CategoryDTO"></select>
	
	<select id="subCategoryList" resultType="SubCategoryDTO"></select>
	
</mapper>
